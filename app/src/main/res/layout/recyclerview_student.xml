<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:id="@+id/list_student"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/tv_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:padding="2dp"
                android:textColor="@android:color/black"
                android:textSize="20sp"
                android:contentDescription="Nama Mahasiswa"
                android:textStyle="bold"
                tools:text="Nama" />

            <TextView
                android:id="@+id/tv_idClass"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:padding="2dp"
                android:contentDescription="Nim dan Kelas"
                android:textColor="#474747"
                android:textSize="16sp"
                tools:text="NIM - Kelas" />

            <TextView
                android:id="@+id/tv_statusGradeScore"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:padding="2dp"
                android:contentDescription="Nilai Indeks Akhir"
                android:textColor="#474747"
                android:textSize="12sp"
                tools:text="Status [Indeks (Nilai)]" />
        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="0.2dp"
            android:background="@android:color/black"/>

    </LinearLayout>
</layout>
